{
  "builds": [
    {
      "src": "front/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "front/apps/webs/dist"
      },
  
      "routes": [
        {
          "src": "/(.*)",
          "dest": "/$1"
        }
      ]
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "PACKAGE_MANAGER": "pnpm"
  }
}
